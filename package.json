{
  "name": "pi-agent-history-recall",
  "version": "0.2.0",
  "description": "Pi extension: project conversation retrieval from Session JSONL (evidence, not memory)",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi-extension",
    "history",
    "retrieval",
    "session",
    "exploration"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test tests"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": "^0.64.0",
    "typebox": "^1.1.24"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.8.3"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  }
}
